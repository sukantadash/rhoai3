apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSAuthPolicy
metadata:
  name: "free-models-access"
  namespace: "models-as-a-service"
  labels:
    migration.maas.opendatahub.io/from-tier: "free"
    migration.maas.opendatahub.io/generated: "true"
  annotations:
    migration.maas.opendatahub.io/original-tier: "free"
    description: "Access policy for free tier models (migrated from tier-based system)"
spec:
  modelRefs:
    - name: "simulated-free"
      namespace: "ai-models"
    - name: "my-external-model"
      namespace: "ai-models"
  subjects:
    groups:
      - name: "system:authenticated"
    users: []
