apiVersion: v1
kind: Secret
metadata:
  name: external-model-credentials
  namespace: ai-models
  labels:
    inference.networking.k8s.io/bbr-managed: "true"
type: Opaque
stringData:
  api-key: "REPLACE_ME"
