apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - telemetry-policy.yaml
  - istio-telemetry.yaml
  # Limitador metrics are scraped via Kuadrant observability + UWM, not RHOAI Prometheus.
  # servicemonitor-limitador.yaml is kept for reference only — do not apply.

labels:
  - pairs:
      app.kubernetes.io/part-of: maas-observability
