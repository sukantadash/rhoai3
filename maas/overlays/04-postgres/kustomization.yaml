# MaaS namespace and MaaS resources (API, routes, auth, rate limits).
# Apply after DSC and gateway are in place.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/instances/postgres
